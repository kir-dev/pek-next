<div id="content-main" class="neutral-content">
<div class="uk-width-medium-1-2 uk-container-center uk-overflow-container">

  <h1>Elsődleges kör beállítás</h1>

  <p>SVIE köreid:</p>
  <%= form_tag(svie_update_path, name: 'membershipform') do %>
    <!-- Form name should always be given: https://stackoverflow.com/questions/4831848/firefox-ignores-option-selected-selected -->
      <%= select :svie, :primary_membership, @svie_memberships.map { |m| [m.group.name, m.id] },
        selected: current_user.svie_primary_membership %>
    <%= submit_tag 'Mentés', class: 'uk-button uk-button-primary uk-margin-left' %>
  <% end %>

</div>
</div>

<div class="uk-width-1-1 uk-width-medium-1-3">
  <div class="uk-panel uk-panel-box dark hover group-search" onclick="window.location='<%= group_path(group.id) %>'">
    <div class="uk-panel-title uk-margin-small-bottom">
      <%= link_to group.name, group_path(group.id), class: 'uk-text-primary' %>
    </div>
    <%= group.truncated_description %>
  </div>
</div>
